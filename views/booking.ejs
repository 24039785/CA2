<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Your Booking Cart - Flare Air</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        padding: 40px;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background: #fff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      h2 {
        text-align: center;
        margin-bottom: 30px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
      }
      th,
      td {
        padding: 12px;
        border: 1px solid #ccc;
        text-align: center;
      }
      img {
        max-width: 80px;
        border-radius: 4px;
      }
      a.button {
        display: inline-block;
        padding: 12px 20px;
        margin-top: 20px;
        background-color: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 4px;
      }
      a.button:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Your Booking Cart</h2>
      <% if (!booking || booking.length === 0) { %>
      <p>Your booking cart is empty.</p>
      <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Location</th>
            <th>Image</th>
            <th>Quantity</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i = 0; i < booking.length; i++) { %>
          <tr>
            <td><%= booking[i].location %></td>
            <td>
              <img
                src="/images/<%= booking[i].image %>"
                alt="<%= booking[i].location %>"
              />
            </td>
            <td><%= booking[i].quantity %></td>
          </tr>
          <% } %>
        </tbody>
      </table>
      <a href="/booking" class="button">Back to Booking</a>
      <% } %>
    </div>
  </body>
</html>

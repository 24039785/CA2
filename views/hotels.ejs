<!-- product.ejs -->

<!DOCTYPE html>
<html lang="en">

<head>
  <!--navbar-->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navigation</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/admin">Hotels Available</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/add">Add Hotel</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/edit-users">Edit User</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/AdminSearch">Search Hotel</a>
          </li>
        </ul>
        <!-- Logout Button -->
        <a href="/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>
<<<<<<< HEAD:views/AdminSearch.ejs
  </nav>
  <!--navbar -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - Travelly</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background-color: #f8f9fa;
    }

    .card-img-top {
      height: 200px;
      object-fit: cover;
    }

    .availability {
      font-weight: bold;
    }

    .admin-actions {
      margin-top: 10px;
    }
  </style>
</head>

<body>
  <div class="container mt-4">
    <h2>Search a Hotel</h2>

    <form method="GET" action="/AdminSearch">
      <input class="form-control mb-2" name="hotel" type="search" placeholder="Search a Hotel" aria-label="hotel name"
        style="max-width: 250px;">
      <select class="form-select mb-2" name="roomSize" aria-label="Select Room Size" style="max-width: 250px;">
        <option selected value="">Room Size</option>
        <option value="Small">Small</option>
        <option value="Medium">Medium</option>
        <option value="Large">Large</option>
      </select>
      <button class="btn btn-primary" type="submit">Search</button>
    </form>
  </div>

  <h3>Search Results:</h3>

  <% if (bookings.length===0) { %>
    <p>No results found.</p>
    <% } else { %>
      <ul>
        <% bookings.forEach(bookings=> { %>
          <li>
            <%= bookings.name %> - <%= bookings.roomType %>
          </li>
          <% }) %>
      </ul>
      <% } %>
=======
  </div>
</nav>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .availability {
            font-weight: bold;
        }
        .admin-actions {
            margin-top: 10px;
        }
    </style>
<!--navbar -->
>>>>>>> 3ce26ad9c614ce61a89399ea2ccfc8f51d0894bd:views/hotels.ejs

    <div class="container">
        <div class="text-center"><h2>Hotel Details</h2></div>
        <% if (bookings) { %> <!-- Check if hotel data exists -->
            <div>
                <p><b>Hotel Name:</b><br> <%= bookings.name %></p>
                <p><b>Image:</b><br> <img src = "../images/<%=bookings.image %>" width="40%"></p>
                <p><b>Location:</b><br> <%= bookings.location %></p>
                <p><b>Room Type:</b><br><%= bookings.roomType %></p> 
            </div>
        <% } else { %>
            <p>No hotel found.</p>
        <% } %>
        <% if (users.role === "admin") { %>
          <a href ="/admin">Back</a>
        <% } else { %>
          <a href ="/admin">Back</a>
        <% } %>
    </div>
    
</body>
</html>

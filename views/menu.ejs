<!DOCTYPE html>
<html>
<head>
  <title>Search your hotel</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      text-align: center;
      margin-top: 100px;
    }

    h2 {
      color: #333;
    }

    #searchInput {
      padding: 10px;
      width: 250px;
      font-size: 16px;
      border: 1px solid #aaa;
      border-radius: 5px;
    }

    button {
      padding: 10px 15px;
      font-size: 16px;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 5px;
      margin-left: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }

    #result {
      margin-top: 20px;
      font-size: 18px;
      color: #444;
    }
  </style>
</head>
<body>

  <h2>Search</h2>

  <input type="text" id="searchInput" placeholder="Type something...">
  <button onclick="checkInput()">Search</button>

  <p id="result"></p>

  <script>
    function checkInput() {
      var input = document.getElementById("searchInput").value;
      var resultText = "";

      if (input === hotelname) {
        result = ;
      }

      if (input === "car") {
        resultText = "You searched for: A car is a vehicle.";
      }

      if (input === "cat") {
        resultText = "You searched for: A cat is an animal.";
      }

      if (input !== "apple" && input !== "car" && input !== "cat") {
        resultText = "No result found.";
      }

      document.getElementById("result").innerText = resultText;
    }
  </script>

</body>
</html>